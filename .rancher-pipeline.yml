stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: blueforest/web:1.8.5
      pushRemote: true
      registry: index.docker.io
timeout: 60
