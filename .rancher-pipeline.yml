stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: blueforest/web:1.9.8
      pushRemote: true
      registry: index.docker.io
timeout: 60
